#!/bin/bash

chmod +x /start.sh
chmod +x /usr/bin/launcher
chmod +x /usr/bin/log
chmod +x /usr/bin/reaper
chmod +x /usr/bin/session-manager
chmod +x /usr/bin/steamos-session-select
chmod +x /usr/bin/switch-session

mkdir -p /etc/gamescope/scripts